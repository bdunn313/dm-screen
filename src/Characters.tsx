import React from "react";
import { Typography } from "@material-ui/core";

function Characters() {
  return (
    <main>
      <Typography variant="body1">Here's where we stick characters</Typography>
    </main>
  );
}

export default Characters;
